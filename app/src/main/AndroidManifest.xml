<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ribal.cutline">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activity.OrderAcceptActivity"></activity>
        <activity android:name=".activity.ChatActivity" />
        <activity android:name=".activity.ChatRoomActivity" />
        <activity android:name=".activity.ChatRoomBarberActivity" />
        <activity android:name=".activity.ReservationDetailActivity" />
        <activity android:name=".activity.ReservationDetailCompleteActivity" />
        <activity android:name=".activity.OrderDetailActivity" />
        <activity android:name=".activity.OrderDetailCompleteActivity" />
        <activity android:name=".activity.OrderRequestActivity" />
        <activity android:name=".activity.OrderHistoryActivity" />
        <activity android:name=".activity.ReservationActivity" />
        <activity android:name=".activity.AddImageBusiness" />
        <activity android:name=".activity.BarberPageActivity" />
        <activity android:name=".activity.FindBarberActivity" />
        <activity android:name=".activity.CreateBusinessActivity" />
        <activity android:name=".activity.ManageBusinessActivity" />
        <activity android:name=".activity.EditProfile" />
        <activity android:name=".activity.MainActivityUser" />
        <activity android:name=".activity.RegisterForm" />
        <activity android:name=".activity.Register" />
        <activity android:name=".activity.Login" />
        <activity android:name=".activity.MainActivityBarber" />
        <activity android:name=".activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.ribal.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>